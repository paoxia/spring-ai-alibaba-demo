spring:
  application:
    name: spring-ai-alibaba-demo
  ai:
    # OpenAI API 配置（使用 OpenAI 兼容接口）
    openai:
      api-key: ${OPENAI_MODE_API_KEY:your-api-key-here}
      base-url: https://ark.cn-beijing.volces.com/api/coding
      chat:
        options:
          model: glm-4.7
          temperature: 0.7
          top-p: 0.9
          max-tokens: 2000

server:
  port: 8081

logging:
  level:
    root: INFO
    com.alibaba.cloud.ai: DEBUG
    org.springframework.ai: DEBUG
